\documentclass[letterpaper,10pt]{article}
\usepackage[hmargin=0.5in,vmargin=0.5in]{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{marvosym}
\usepackage[hidelinks]{hyperref}
\usepackage{fontspec}
\usepackage{color}

\definecolor{primary}{HTML}{2b2b2b}
\definecolor{headings}{HTML}{6A6A6A}
\definecolor{subheadings}{HTML}{333333}

\pagestyle{empty}

% Section formatting
\titleformat{\section}{\color{headings}\scshape\large\raggedright}{}{0em}{}[\color{headings}\titlerule \vspace{-3pt}]
\titlespacing{\section}{0pt}{8pt}{4pt}

\newcommand{\experienceHeading}[4]{
  \vspace{2pt}
  \textbf{#1} $|$ \textit{#2} \hfill \small{#3 -- #4} \\
}

\begin{document}

% HEADER
\begin{center}
  {\Huge \scshape \color{primary} {{ full_name }}} \\ \vspace{2pt}
  \small 
  {% if phone %}{{ phone }} $|$ {% endif %}
  \href{mailto:{{ email }}}{ {{ email }} } 
  {% if website %} $|$ \href{ {{ website }} }{ {{ website }} } {% endif %}
  {% if github_url %} $|$ \href{ {{ github_url }} }{github.com/{{ github_url.split('/')[-1] if '/' in github_url else github_url }} } {% endif %}
  {% if linkedin_url %} $|$ \href{ {{ linkedin_url }} }{linkedin.com/in/{{ linkedin_url.split('/')[-1] if '/' in linkedin_url else linkedin_url }} } {% endif %}
\end{center}

\vspace{5pt}

% LEFT COLUMN (SIDEBAR)
\begin{minipage}[t]{0.28\textwidth}
  \raggedright
  
  \section{Education}
  {% for edu in education %}
    \vspace{2pt}
    \textbf{ {{ edu.institution }} } \\
    \small{ {{ edu.degree }} } \\
    \small{ {{ edu.field_of_study }} } \\
    \small{ {{ edu.start_date }} -- {{ edu.end_date or "Present" }} } \\
    {% if edu.gpa %}\small{GPA: {{ edu.gpa }}} \\ {% endif %}
    \vspace{4pt}
  {% endfor %}

  \section{Skills}
  {% for s in skills %}
    \small{ $\bullet$ \textbf{ {{ s.name }} } } \\
  {% endfor %}

  \vspace{5pt}
  
  {% if certifications %}
    \section{Certifications}
    {% for cert in certifications %}
      \small{ $\bullet$ {{ cert.name }} } \\
      \textit{\footnotesize {{ cert.issuer }} } \\
      \vspace{2pt}
    {% endfor %}
  {% endif %}

\end{minipage}
\hfill
% RIGHT COLUMN (MAIN CONTENT)
\begin{minipage}[t]{0.68\textwidth}
  \raggedright

  \section{Summary}
  \small{
    {{ summary }}
  }
  
  \vspace{5pt}

  \section{Experience}
  {% for exp in experience %}
    \experienceHeading{ {{ exp.company }} }{ {{ exp.position }} }{ {{ exp.start_date }} }{ {{ exp.end_date or "Present" }} }
    \begin{itemize}[leftmargin=0.15in, topsep=1pt, itemsep=1pt, parsep=0pt]
      {% for bullet in exp.bullets %}
        \item \small{ {{ bullet }} }
      {% endfor %}
    \end{itemize}
    \vspace{4pt}
  {% endfor %}

  \section{Projects}
  {% for proj in projects %}
    \experienceHeading{ {{ proj.name }} }{ {% if proj.github_url %}GitHub{% else %}Project{% endif %} }{ {{ proj.start_date or "" }} }{ {{ proj.end_date or "" }} }
    \begin{itemize}[leftmargin=0.15in, topsep=1pt, itemsep=1pt, parsep=0pt]
      {% for bullet in proj.bullets %}
        \item \small{ {{ bullet }} }
      {% endfor %}
    \end{itemize}
    \vspace{4pt}
  {% endfor %}

  {% if achievements %}
    \section{Achievements}
    \begin{itemize}[leftmargin=0.15in, topsep=1pt, itemsep=1pt, parsep=0pt]
      {% for ach in achievements %}
        \item \small{ \textbf{ {{ ach.title }} } -- {{ ach.description }} }
      {% endfor %}
    \end{itemize}
  {% endif %}

\end{minipage}

\end{document}
