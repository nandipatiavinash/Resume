\documentclass[letterpaper,11pt]{article}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{xcolor}
\usepackage{fontspec}

% Colors
\definecolor{accentcolor}{HTML}{0056b3} % Professional blue accent
\definecolor{darkgray}{HTML}{333333}

\pagestyle{empty}

% Margins
\addtolength{\oddsidemargin}{-0.5in}
\addtolength{\evensidemargin}{-0.5in}
\addtolength{\textwidth}{1.0in}
\addtolength{\topmargin}{-.5in}
\addtolength{\textheight}{1.0in}

\urlstyle{same}

% Section formatting
\titleformat{\section}{
  \vspace{-4pt}\scshape\raggedright\large\color{accentcolor}
}{}{0em}{}[\color{accentcolor}\titlerule \vspace{-5pt}]

% Custom commands
\newcommand{\resumeItem}[1]{
  \item\small{
    {#1 \vspace{-2pt}}
  }
}

\newcommand{\resumeSubheading}[4]{
  \vspace{-2pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{\color{darkgray}#1} & #2 \\
      \textit{\small#3} & \textit{\small #4} \\
    \end{tabular*}\vspace{-7pt}
}

\newcommand{\resumeSubHeadingListStart}{\begin{itemize}[leftmargin=0.15in, label={}]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}
\newcommand{\resumeItemListStart}{\begin{itemize}[label={\color{accentcolor}$\bullet$}]}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-5pt}}

\begin{document}

%----------HEADING----------
\begin{center}
    \textbf{\Huge \scshape \color{accentcolor} {{ full_name }}} \\ \vspace{4pt}
    \small 
    {% if phone %}\color{darkgray}{{ phone }} $|$ {% endif %}
    \href{mailto:{{ email }}}{ {{ email }} } 
    {% if website %} $|$ \href{ {{ website }} }{Website} {% endif %}
    {% if github_url %} $|$ \href{ {{ github_url }} }{GitHub} {% endif %}
    {% if linkedin_url %} $|$ \href{ {{ linkedin_url }} }{LinkedIn} {% endif %}
\end{center}

%-----------SUMMARY-----------
\section{Summary}
\small{
  \color{darkgray}{{ summary }}
}

%-----------EXPERIENCE-----------
\section{Professional Experience}
  \resumeSubHeadingListStart
    {% for exp in experience %}
      \resumeSubheading
        { {{ exp.company }} }{ {{ exp.location or "" }} }
        { {{ exp.position }} }{ {{ exp.start_date }} -- {{ exp.end_date or "Present" }} }
        \resumeItemListStart
          {% for bullet in exp.bullets %}
            \resumeItem{ {{ bullet }} }
          {% endfor %}
        \resumeItemListEnd
    {% endfor %}
  \resumeSubHeadingListEnd

%-----------PROJECTS-----------
\section{Key Projects}
  \resumeSubHeadingListStart
    {% for proj in projects %}
      \item
      \begin{tabular*}{0.97\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{\color{darkgray}{{ proj.name }} } {% if proj.github_url %}$|$ \href{ {{ proj.github_url }} }{\small Source} {% endif %} & {{ proj.start_date or "" }} \\
      \end{tabular*}\vspace{-7pt}
      \resumeItemListStart
        {% for bullet in proj.bullets %}
          \resumeItem{ {{ bullet }} }
        {% endfor %}
      \resumeItemListEnd
    {% endfor %}
  \resumeSubHeadingListEnd

%-----------EDUCATION-----------
\section{Education}
  \resumeSubHeadingListStart
    {% for edu in education %}
      \resumeSubheading
        { {{ edu.institution }} }{ {{ edu.degree }} }
        { {{ edu.field_of_study }} }{ {{ edu.start_date }} -- {{ edu.end_date or "Present" }} }
    {% endfor %}
  \resumeSubHeadingListEnd

%-----------SKILLS-----------
\section{Skills \& Expertises}
 \begin{itemize}[leftmargin=0.15in, label={}]
    \small{\item{
     {% for s in skills %}
       \textbf{\color{darkgray}{{ s.name }}} ({% if s.category %}{{ s.category }}{% else %}Skill{% endif %}){% if not loop.last %} $|$ {% endif %}
     {% endfor %}
    }}
 \end{itemize}

%-----------CERTIFICATIONS-----------
{% if certifications %}
\section{Certifications}
  \begin{itemize}[leftmargin=0.15in, label={\color{accentcolor}$\bullet$}]
    {% for cert in certifications %}
      \item \small{
        \textbf{ {{ cert.name }} } -- {{ cert.issuer }} ({% if cert.issue_date %}{{ cert.issue_date }}{% endif %})
      }
    {% endfor %}
  \end{itemize}
{% endif %}

\end{document}
